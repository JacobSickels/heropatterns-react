import * as React from 'react';
import { HeroPatternProps } from '../HeroPattern';

export default class HeroPattern extends React.Component<HeroPatternProps> {
    renders() {
        return <div>Hello world!</div>;
    }
}
